define([], function () {
    'use strict';

    return function() {
        return {
            restrict: 'A',
            link: function(scope, element, attrs, req, transclude) {

            }
        }
    };
});
